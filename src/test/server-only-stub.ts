// Test stub for the `server-only` package. The real module throws when imported
// outside a React Server Component build; in vitest we just want a no-op so
// server modules can be unit-tested. Aliased via vitest.config.ts.
export {};
