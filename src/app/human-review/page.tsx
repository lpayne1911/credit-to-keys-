import { IntakeForm } from "@/components/products/IntakeForm";
export const metadata = { title: "Human review — Driveway Advocate" };
export default function HumanReviewPage() {
  return <IntakeForm productId="human-review" />;
}
